# Zustand-State-Management-Example
Built a Zustand state management app following a React course tutorial.
*NOTE: I added a unique "key" prop to the .map() function that was not included in the tutorial.
